services:
  scraper:
    image: golang:1.26-rc-alpine
    container_name: go_notion_scraper_dev
    working_dir: /app
    volumes:
      - .:/app
    env_file:
      - .env
    tty: true
    dns:
      - 8.8.8.8
      - 8.8.4.4
    command: go run main.go